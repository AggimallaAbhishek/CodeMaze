name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main"]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r apps/api/requirements.txt

      - name: Lint Python
        run: |
          flake8 apps/api workers/engine

      - name: Format check (black)
        run: |
          black --check apps/api workers/engine

      - name: Security scan (Bandit)
        run: |
          bandit -r apps/api workers/engine

      - name: Run backend tests
        env:
          DJANGO_SETTINGS_MODULE: puzzle_api.settings
          PYTHONPATH: ${{ github.workspace }}/apps/api:${{ github.workspace }}/workers/engine
          SECRET_KEY: test-secret
          DEBUG: true
          ALLOWED_HOSTS: localhost,127.0.0.1,testserver
          REDIS_URL: redis://localhost:6379/0
        run: |
          cd apps/api
          pytest

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install frontend dependencies
        run: |
          cd apps/web
          npm ci

      - name: Lint frontend
        run: |
          cd apps/web
          npm run lint

      - name: Run frontend unit tests
        run: |
          cd apps/web
          npm run test -- --run

      - name: Build frontend and emit bundle report
        run: |
          cd apps/web
          npm run build:report

      - name: Upload bundle report
        uses: actions/upload-artifact@v4
        with:
          name: web-bundle-report
          path: apps/web/dist/bundle-report.json

      - name: Install Playwright Browser
        run: |
          cd apps/web
          npx playwright install --with-deps chromium

      - name: Run Playwright desktop E2E tests
        run: |
          cd apps/web
          npm run test:e2e:desktop

      - name: Run Playwright mobile E2E tests
        run: |
          cd apps/web
          npm run test:e2e:mobile

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v11
        with:
          configPath: apps/web/lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true

      - name: Security audit
        run: |
          cd apps/web
          npm audit --audit-level=high
